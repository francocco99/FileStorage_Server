#include <stdlib.h>
#include <stdio.h>
#include <string.h>
#include <stdarg.h>
int parse_str(char*** ris,char* ric, const char* delim);
char * strc(const char* s1,char* s2);
char * concat(char* str, ...);